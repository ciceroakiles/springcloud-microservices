server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ciceroakiles/springcloud-microservices/tree/master/config-server/src/main/java/com/sample/configserver/config
